<% @titile = 'new resister' %>
<h1><%= @title %></h1>

<div id="generic-form">
	<% form_for @sales_form, as: 'form', url: cost_sales do |f| %>
		<% render 'form', f: f %>
		<div class= "buttons">
			<%= f.submit 'register' %>	
			<%= link_to 'cancel', :cost_sales %>
		</div>
	<% end %>
</div>

